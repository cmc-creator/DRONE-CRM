# =============================================================================
# NyxAerial CRM — Environment Variables
# Copy this file to .env.local and fill in your values.
# All variables are optional unless marked [REQUIRED].
# =============================================================================

# ─── Database ─────────────────────────────────────────────────────────────────

# [REQUIRED] PostgreSQL connection string (Neon, Railway, Supabase, etc.)
DATABASE_URL="postgresql://user:password@host:5432/dbname?sslmode=require"

# Direct connection used only by prisma db push / migrations (if using Neon pooler)
# DIRECT_URL="postgresql://user:password@host:5432/dbname"

# ─── NextAuth ─────────────────────────────────────────────────────────────────

# [REQUIRED] Random secret: run `openssl rand -base64 32` to generate
NEXTAUTH_SECRET="your-secret-here"

# [REQUIRED in production] Your full deployment URL (no trailing slash)
NEXTAUTH_URL="http://localhost:3000"
AUTH_URL="http://localhost:3000"

# ─── White-Label Brand ────────────────────────────────────────────────────────

# Customize the CRM identity — all sidebar/email references use these values.
# Defaults fall back to "NyxAerial" if not provided.
NEXT_PUBLIC_ORG_NAME="NyxAerial"
NEXT_PUBLIC_ORG_TAGLINE="Drone Operations Platform"
NEXT_PUBLIC_ORG_WEBSITE="https://nyxaerial.com"
NEXT_PUBLIC_ORG_EMAIL="ops@nyxaerial.com"

# ─── Email (Resend) ───────────────────────────────────────────────────────────

# Transactional email via Resend (https://resend.com — 3,000 free emails/month)
RESEND_API_KEY="re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
EMAIL_FROM="NyxAerial <onboarding@resend.dev>"

# ─── Stripe Payments ──────────────────────────────────────────────────────────

STRIPE_SECRET_KEY="sk_live_..."
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_live_..."
STRIPE_WEBHOOK_SECRET="whsec_..."

# ─── Google Cloud / Workspace ─────────────────────────────────────────────────

# OAuth credentials — used for Google Drive, Calendar, and optional Google SSO
# console.cloud.google.com → Credentials → OAuth 2.0 Client IDs
GOOGLE_CLIENT_ID="your-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="GOCSPX-..."

# Optional: sync jobs to this Google Calendar ID
# GOOGLE_CALENDAR_ID="primary"

# Optional: Google Maps API key (for production job map views)
# NEXT_PUBLIC_GOOGLE_MAPS_KEY="AIza..."

# Google Analytics
# NEXT_PUBLIC_GA_MEASUREMENT_ID="G-XXXXXXXXXX"

# ─── Microsoft Azure / Microsoft 365 ─────────────────────────────────────────

# Azure App Registration — used for OneDrive, SharePoint, Teams, Outlook, and Azure AD SSO
# portal.azure.com → App Registrations → New Registration
MICROSOFT_CLIENT_ID="xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
MICROSOFT_CLIENT_SECRET="your-azure-client-secret"
# Use "common" for both personal and work/school accounts, or your tenant GUID
MICROSOFT_TENANT_ID="common"

# SharePoint document library site URL
# SHAREPOINT_SITE_URL="https://contoso.sharepoint.com/sites/drone"

# Microsoft Teams Incoming Webhook (Teams channel → Connectors → Incoming Webhook)
# TEAMS_WEBHOOK_URL="https://xxx.webhook.office.com/webhookb2/..."

# Outlook/Exchange send-from address (requires Mail.Send Graph permission)
# OUTLOOK_SENDER_EMAIL="ops@yourdomain.com"

# ─── E-Signature ──────────────────────────────────────────────────────────────

# Adobe Acrobat Sign
# ADOBE_SIGN_WEBHOOK_SECRET="your-adobe-sign-secret"

# DocuSign Connect
# DOCUSIGN_INTEGRATION_KEY="your-docusign-integration-key"
# DOCUSIGN_ACCOUNT_ID="your-docusign-account-guid"
# DOCUSIGN_HMAC_SECRET="your-docusign-hmac-secret"
# DOCUSIGN_BASE_URL="https://na4.docusign.net/restapi"

# PandaDoc
# PANDADOC_API_KEY="your-pandadoc-api-key"
# PANDADOC_WEBHOOK_SECRET="your-pandadoc-webhook-secret"

# ─── Cloud Storage ────────────────────────────────────────────────────────────

# Amazon S3 / Cloudflare R2 (S3-compatible)
# S3_ACCESS_KEY_ID="AKIAIOSFODNN7EXAMPLE"
# S3_SECRET_ACCESS_KEY="wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY"
# S3_BUCKET="your-bucket-name"
# S3_REGION="us-east-1"
# S3_ENDPOINT=""   # Leave empty for AWS; set to your R2 endpoint for Cloudflare R2

# Dropbox (dropbox.com/developers → My Apps → Create App)
# DROPBOX_APP_KEY="your-dropbox-app-key"
# DROPBOX_APP_SECRET="your-dropbox-app-secret"

# Box
# BOX_CLIENT_ID="your-box-client-id"
# BOX_CLIENT_SECRET="your-box-client-secret"

# ─── Communication ─────────────────────────────────────────────────────────────

# Slack Incoming Webhook (api.slack.com → Your Apps → Incoming Webhooks)
# SLACK_WEBHOOK_URL="https://hooks.slack.com/services/T.../B.../..."

# Twilio SMS
# TWILIO_ACCOUNT_SID="ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
# TWILIO_AUTH_TOKEN="your-auth-token"
# TWILIO_FROM_NUMBER="+12025551234"

# SendGrid (alternative to Resend)
# SENDGRID_API_KEY="SG.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"

# Mailchimp
# MAILCHIMP_API_KEY="xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx-us21"
# MAILCHIMP_LIST_ID="your-list-id"
# MAILCHIMP_SERVER="us21"

# ─── Accounting ───────────────────────────────────────────────────────────────

# QuickBooks Online (developer.intuit.com → My Apps)
# QUICKBOOKS_CLIENT_ID="your-qbo-client-id"
# QUICKBOOKS_CLIENT_SECRET="your-qbo-client-secret"
# QUICKBOOKS_ENV="sandbox"   # sandbox | production

# FreshBooks
# FRESHBOOKS_CLIENT_ID="your-freshbooks-client-id"
# FRESHBOOKS_CLIENT_SECRET="your-freshbooks-client-secret"

# Xero
# XERO_CLIENT_ID="your-xero-client-id"
# XERO_CLIENT_SECRET="your-xero-client-secret"

# ─── Automation ───────────────────────────────────────────────────────────────

# Zapier
# ZAPIER_WEBHOOK_URL="https://hooks.zapier.com/hooks/catch/..."
# ZAPIER_INBOUND_SECRET="random-string-for-verification"

# Make (Integromat)
# MAKE_WEBHOOK_URL="https://hook.eu1.make.com/..."

# Airtable
# AIRTABLE_TOKEN="your-airtable-personal-access-token"
# AIRTABLE_BASE_ID="appXXXXXXXXXXXXXX"

# Custom outbound webhook
# CUSTOM_WEBHOOK_URL="https://your-system.com/webhook"
# CUSTOM_WEBHOOK_SECRET="random-secret-for-X-Signature-header"

# ─── CRM & Lead Capture ────────────────────────────────────────────────────────

# HubSpot
# HUBSPOT_API_KEY="pat-na1-xxxxxxxx-xxxx-xxxx-xxxxxxxx"

# Salesforce
# SALESFORCE_ACCESS_TOKEN="your-connected-app-token"

# Typeform
# TYPEFORM_WEBHOOK_SECRET="your-typeform-webhook-secret"

# Webflow
# WEBFLOW_API_TOKEN="your-webflow-api-token"

# ─── FAA & Airspace ────────────────────────────────────────────────────────────

# AirMap
# AIRMAP_API_KEY="your-airmap-api-key"

# OpenAIP
# OPENAIP_API_KEY="your-openaip-api-key"

# Aloft / SkyGrid
# ALOFT_API_KEY="your-aloft-api-key"
